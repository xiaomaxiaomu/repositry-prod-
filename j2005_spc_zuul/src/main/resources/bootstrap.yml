spring:
  application:
    name: zuulservice
  cloud:
    config:
      discovery:
        enabled: true
        service-id: remoteconfigservice
  #      uri: http://localhost:8888
#      fail-fast: true
  profiles:
    active: dev

